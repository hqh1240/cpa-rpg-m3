# UI 设计约束

## 地图实体标签

- 禁止在地图实体上方绘制矩形背景框。
- 禁止使用大面积黄色/金色描边作为任务标签。
- 地图实体名称使用无背景文字 + 深色阴影，保持轻量可读。
- 待交付任务使用名称右侧的小圆点提示，不额外绘制边框。

## HUD 任务卡

- 任务卡只使用左侧金色细条作为主线/任务标识。
- 不叠加 RPGUI 九宫格边框，避免出现粗黄框。

## 后续开发要求

- 新增任何地图标签、任务提示、HUD 卡片时，先对照本文件。
- 若视觉上出现矩形框、粗边框、大面积金色描边，默认视为不合格。
